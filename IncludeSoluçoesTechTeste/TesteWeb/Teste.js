/*function ordenarNumeros(array) {
    // Verificar se o array tem exatamente dois elementos
    if (array.length !== 2) {
        return "O array precisa ter exatamente dois números.";
    }

    // Atribuir os números do array a variáveis separadas
    let num1 = array[0];
    let num2 = array[1];

    // Verificar qual número é maior
    if (num1 > num2) {
        // Trocar os números de lugar se estiverem fora de ordem
        [num1, num2] = [num2, num1];
    }

    // Retornar um novo array com os números em ordem crescente
    return [num1, num2];
}

// Exemplo de uso da função
let numeros = [5, 2];
console.log("Array original:", numeros);
console.log("Array ordenado:", ordenarNumeros(numeros));*/

let meuArray = [1, 2, 3, 4, 5];

console.log("numero de array", meuArray.length); // Saída: 5

